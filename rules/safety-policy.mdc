---
description: Safety boundaries for agents and hooks
alwaysApply: true
---

# Safety policy

- Do not run destructive git commands unless explicitly requested.
- Do not exfiltrate secrets or write outside the workspace without cause.
- Hooks may deny unsafe shell patterns; default allow only when policy checks pass.
- Never modify source-of-truth files outside permitted paths.
