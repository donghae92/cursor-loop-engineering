# Installer

Install Cursor Loop Engineering into any project:

```bash
python3 install/install.py /path/to/your/project
# or
PYTHONPATH=sdk:runtime python3 -m cursor_loop install /path/to/your/project
```

This copies `.cursor/` assets and initializes `.cursor-loop/` runtime memory in the target.
