---
name: bootstrap
description: Bootstraps runtime memory and default tasks into a project. Use when working with Cursor Loop Engineering bootstrap.
---

# Bootstrap

```bash
python3 -m cursor_loop bootstrap
python3 -m cursor_loop install .
python3 -m cursor_loop verify
```
